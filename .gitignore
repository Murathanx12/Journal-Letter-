# dependencies
node_modules/
.pnp
.pnp.*

# next.js
.next/
out/
build/
next-env.d.ts

# testing
coverage/
playwright-report/
test-results/
.playwright/

# environment — never commit real secrets
.env
.env.local
.env.*.local
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# supabase
supabase/.temp/
supabase/.branches/

# Regenerated by `next dev` and `next build` on every run, from the installed
# version of Next.js. Committing them would mean a diff whenever the framework
# is upgraded, for a file nobody edits.
AGENTS.md
CLAUDE.md

# os / editor
.DS_Store
Thumbs.db
*.swp
.idea/
